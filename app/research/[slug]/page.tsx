import {notFound} from 'next/navigation';import {Header,Footer,CTA} from '../../components';import {researchPosts} from '../../data';export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}export default async function ResearchPost({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);if(!p)notFound();return <><Header/><main className="section"><article className="container guide-article"><p className="eyebrow">Philippines staffing research · Updated {p.updated}</p><h1>{p.title}</h1><p className="lead">{p.excerpt}</p><img src={p.image} alt={p.imageAlt} style={{width:'100%',height:'auto',borderRadius:12,margin:'1.5rem 0'}} />{p.body.map(x=><p key={x}>{x}</p>)}<div className="card"><h2>Sources</h2><ul>{p.sources.map(s=><li key={s.href}><a href={s.href} rel="noreferrer">{s.label}</a></li>)}</ul><h2>Continue the research</h2><ul>{p.related.map(s=><li key={s.href}><a href={s.href}>{s.label}</a></li>)}</ul></div></article><CTA/></main><Footer/></>}
