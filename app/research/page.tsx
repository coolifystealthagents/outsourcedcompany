import {Header,Footer} from '../components';import {allResearchPosts,site} from '../data';export const metadata={title:`Research | ${site.brand}`,description:'Sourced research notes for Philippines-based staffing and operations decisions.'};export default function Research(){return <><Header/><main className="section"><div className="container"><p className="eyebrow">Philippines staffing research</p><h1>Research for better role decisions.</h1><p className="lead">Sourced notes about Philippines-based staffing, operations, privacy, quality, and continuity.</p><div className="cards">{allResearchPosts.map(p=><a className="card" href={`/research/${p.slug}`} key={p.slug}><h2>{p.title}</h2><p>{p.excerpt}</p><span>Updated {p.updated}</span></a>)}</div></div></main><Footer/></>}
